<Window x:Class="_12_Login.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:local="clr-namespace:_12_Login.Views"
        mc:Ignorable="d"
        WindowStyle="None"
        MinHeight="600"
        MinWidth="600"
        Title="MainView" Height="600" Width="600">

    <Grid>
        <Frame x:Name="Main"/>

        <StackPanel 
            HorizontalAlignment="Right" 
            Orientation="Horizontal" 
            VerticalAlignment="Top"
            Grid.Row="0" Grid.Column="0">

            <Button Click="minimizeBtn_Click" x:Name="minimizeBtn" >
                <md:PackIcon Kind="Minimize"/>
            </Button>
            <Button Click="maximizeBtn_Click" x:Name="maximizeBtn">
                <md:PackIcon Kind="Maximize"/>
            </Button>
            <Button Click="closeBtn_Click" x:Name="closeBtn" Margin="0, 0, 2, 0">
                <md:PackIcon Kind="Close"/>
            </Button>
        </StackPanel>
    </Grid>
</Window>